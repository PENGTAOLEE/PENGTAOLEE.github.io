sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - main # build master branch only
script:
  - hexo generate # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: 445abd498fcb22189137263f6116b43a9554a613
  keep-history: true
  on:
    branch: gh-pages
  local-dir: public